biopython>=1.81
matplotlib>=3.7.0
numpy>=1.24.0
pandas>=2.0.0
py3Dmol>=2.0.0
requests>=2.28.0
seaborn>=0.12.0
scipy>=1.10.0
# Optional: TMalign binary for TM-score output.
# Install or place TMalign on PATH; for conda users, try:
# conda install -c bioconda tmalign
# Optional: Foldseek binary for structure similarity search and clustering.
# conda install -c conda-forge -c bioconda foldseek
# Foldseek web/API search uses requests+pandas and does not require the local Foldseek binary.
