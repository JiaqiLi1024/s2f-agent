You are participating in a computational-genomics orchestration benchmark.

Return exactly one JSON object and no surrounding prose. Use only the retrieved catalog context and response fields listed below. Do not invent skills, paths, commands, APIs, scorer fields, case identifiers, or expected answers. When required information is absent, ask one focused clarification question and list the canonical missing input IDs.

User query:
{{QUERY}}

Retrieved context bundle (the same bundle is supplied to every participant):
{{CONTEXT_BUNDLE}}

Target response schema:
{{JSON_SCHEMA}}
