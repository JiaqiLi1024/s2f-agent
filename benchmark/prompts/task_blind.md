You are participating in a computational-genomics orchestration benchmark.

Return exactly one JSON object and no surrounding prose. Use only the skill IDs and response fields listed below. Do not invent skills, paths, commands, APIs, or output fields. When required information is absent, ask one focused clarification question and list the canonical missing input IDs.

User query:
{{QUERY}}

Available skill catalog:
{{SKILL_CATALOG}}

Target response schema:
{{JSON_SCHEMA}}
